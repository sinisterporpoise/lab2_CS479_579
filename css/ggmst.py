#------------------------------------------------------------------------
# The GGMSt attempt. Not my preferred choice, but if the class agreed to 
# This I should at least make the effort.
# This is an attempt to redo the C code that is submitted in Python
# using libraries like Numpy and Pandas if needed.
#
# Lara Landis
# CS 691
# Dr. Arash Raifey
# 14 September 2025
#-----------------------------------------------------------------------
import numpy as np


data = ()
#------------------------------------------------------------------------
#
# Here are the classes that have already been put in the main.c file.
# Personally I think this should be a library.
#
#--------------------------------------------------------------------------

# The points in the cell.
class Node:


	def __init__ (self):
		self.x_pos = x_pos;
		self.y_pos = y.pos;
		self.weight = weight;


class Cells:
# the class for cells that contains a list of nodes, with an arbitary maximum of 5 nodes per cell
	
	def __init__(self):
		self.number_of_nodes = number_of_nodes
		self.row_number = row_number
		self.column_number = column_number
		self.cell_nodes = np.zeros(5,2)



#==================================================================================
#
# THIS IS THE MAIN FUNCTION, THIS IS THE MAIN FUNCTION. THIS IS THE MAIN FUNCTION
#
#===================================================================================
def main ():
	cell = Cells()
	cells_list
	n = 5

	for i in range 5:
		cell.number_of_nodes = None 
		cell.colum = None
		cell.row = None 



if __name__ == "__main__":
	main()